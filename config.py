# Файл CONFIG.PY
# -*- coding: utf-8 -*-
# Задаем кодировку UTF-8, чтобы интерпретатор на хостинге Amvera корректно считывал комментарии на русском языке
# Модуль глобальной конфигурации системы HiVolt_DRM_bot
# Версия системы: Beta (v0.4b). Разработано по Протоколу «Alpha 0.2a»

import os  # Импортируем стандартный модуль операционной системы для работы с переменными окружения

# [ТАЙМИНГИ И ИНТЕРВАЛЫ]
# Задержка обновления или перезапуска дашбордов в фоновых процессах (в секундах)
DASH_DELAY = 4.0

# Время жизни бесшумных всплывающих плашек-алертов (answer_callback_query) в группах/супергруппах (в секундах)
# Соответствует пункту 0.2.3 Системного манифеста бизнес-логики бота
ALERT_DURATION = 5.0

# Общее время жизни для всех сервисных автоудаляемых сообщений и окон предупреждений в ЛС (в секундах)
# Соответствует пункту 0.2.2 Системного манифеста бизнес-логики бота
TEMP_MSG_DELAY = 5.0

# [ГЛОБАЛЬНЫЕ СИСТЕМНЫЕ ПАРАМЕТРЫ]
# Текущая эталонная версия программного обеспечения проекта для отображения в панелях управления
VERSION = "v0.4b"

# [ПОЛИТИКА ИЗОЛЯЦИИ КОМАНД]
# Переключатель логики перехвата команд со слэшем (/) на Шаге 3 интерактивного конструктора отчетов
# True — ввод любой команды сбрасывает мастер-конструктор и перенаправляет управление в роутер
# False — любая строка (даже начинающаяся с /) расценивается как обычный текст и заносится в дашборд (п. 4.3 Манифеста)
ALLOW_COMMAND_INTERCEPT = False

# ========== НАСТРОЙКИ ДАШБОРД МЕНЕДЖЕРА ==========
# Приоритеты виджетов (чем меньше число, тем выше в сообщении)
WIDGET_PRIORITY_GREETING = 1
WIDGET_PRIORITY_TECH = 2
WIDGET_PRIORITY_WARNING = 100

# Интервалы пинга (обновление tech_bar) – в секундах
PING_ACTIVE_MIN = 20
PING_ACTIVE_MAX = 60
PING_INACTIVE_MIN = 100
PING_INACTIVE_MAX = 300
ACTIVITY_THRESHOLD = 6000   # время в секундах, после которого группа считается неактивной

# Ширина текста предупреждения в моноширинном шрифте для виджета alert
ALERT_WIDGET_WIDTH = 60

# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL = "DEBUG"


# ========= ПРАВИЛА ДОСТАВКИ УВЕДОМЛЕНИЙ (UserNotifier) =========
# Ключ – категория взаимодействия. Значение – способ показа любого уведомления
# (ошибки, успеха, информации) в рамках этой категории.
#
# Категории:
#   main_dashboard_button    – кнопки главного дашборда в группе (m_dash_*)
#   admin_settings_button    – кнопки панели настроек в ЛС (adm_*)
#   report_wizard_button     – кнопки мастера отчётов (w1_*)
#   group_text_command       – текстовая команда в группе/топике (/help, /mini_games и т.п.)
#   private_text_command     – текстовая команда в ЛС (/reload, /abort и т.п.)
#   external_link_button     – кнопки-ссылки, открывающие URL (обычно не требуют уведомления)
#
# Доступные стратегии:
#   'callback_alert'      – бесшумная плашка (только для callback-кнопок)
#   'dashboard_widget'    – временный виджет alert_placeholder на главном дашборде
#   'private_message'     – временное сообщение в личку (через CanonicalUnifier)
#   'modal'               – модальное окно с кнопкой (только для callback-кнопок)
#   'silent'              – ничего не показывать (только запись в лог)
#
# Если стратегия требует callback (callback_alert, modal), но callback отсутствует
# (например, текстовая команда), автоматически применяется 'private_message'.
# =====================================================================
NOTIFICATION_RULES = {
    'main_dashboard_button': 'callback_alert',    # кнопки главного дашборда – тихие плашки
    'admin_settings_button': 'callback_alert',    # кнопки админ-панели – плашки
    'report_wizard_button': 'callback_alert',     # кнопки мастера отчётов – плашки
    'group_text_command': 'dashboard_widget',     # команды в группе – виджет на дашборде
    'private_text_command': 'private_message',    # команды в ЛС – временные сообщения в ЛС
    'external_link_button': 'silent'              # ссылки – без уведомлений
}



### end of file