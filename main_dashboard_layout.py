### file MAIN_DASHBOARD_LAYOUT.PY ###
# -*- coding: utf-8 -*-
"""
Модуль main_dashboard_layout.py – декларативное описание главного дашборда.

ВНИМАНИЕ! Это файл настроек, а не исполняемый код. Вы можете редактировать его,
чтобы менять внешний вид дашборда в группах: добавлять/удалять текстовые блоки
(виджеты) и кнопки управления.

!!! Никаких знаний Python не требуется – просто следуйте инструкциям в комментариях.
!!! После изменений боту нужна команда /reload, чтобы применить новый макет.

================================================================================
                         СТРУКТУРА КОНФИГУРАЦИИ
================================================================================

Конфигурация состоит из двух частей:
  WIDGETS  – текстовые блоки (сверху вниз).
  BUTTONS  – кнопки под текстом (рядами).

================================================================================
                           ОПИСАНИЕ ВИДЖЕТОВ
================================================================================

WIDGETS – список словарей. Каждый словарь описывает один блок текста.

Поля словаря виджета:
  type          : (строка) Уникальное имя виджета. Используйте латинские буквы
                  и подчёркивания. Примеры: "greeting", "tech_bar", "info".
                  НЕ ИСПОЛЬЗУЙТЕ одноимённые типы для разных виджетов.
  text_key      : (строка или None) Ключ текста в файле STRINGS.py (словарь STRINGS).
                  Если поставить None, то содержимое генерируется автоматически
                  (пока поддерживается только для type="tech_bar").
  priority      : (целое число) Порядок отображения. Чем МЕНЬШЕ число, тем ВЫШЕ
                  виджет находится в сообщении. Рекомендуем: 1 – самый верх,
                  2 – ниже, 3 – ещё ниже и т.д.
  is_permanent  : (True или False) Если True, виджет остаётся на дашборде при
                  принудительном обновлении (/reload, изменение настроек).
                  Если False – удаляется (например, временные предупреждения).

ВАЖНО: Не удаляйте виджеты "greeting" и "tech_bar" без крайней необходимости –
это основные информационные блоки.

   - alert_placeholder: специальный виджет-заглушка для временных предупреждений.
     Поле placeholder_text указывает ключ в STRINGS (или прямой текст), который
     используется как заполнитель по умолчанию. Пример: "alert_placeholder_default".

================================================================================
                           ОПИСАНИЕ КНОПОК
================================================================================

BUTTONS – список РЯДОВ кнопок. Каждый ряд – это СПИСОК кнопок, которые будут
расположены в одной строке. Ряды идут сверху вниз.

Пример для понимания:
  [
    [ кнопка_1 ],                         # первый ряд: одна кнопка на всю ширину
    [ кнопка_2, кнопка_3 ],               # второй ряд: две кнопки рядом
    [ кнопка_4, кнопка_5, кнопка_6 ],     # третий ряд: три кнопки рядом
  ]

Каждая кнопка описывается словарём с полями:

  type          : (строка) Тип кнопки. Возможные значения:
                  "callback"   – кнопка, отправляющая команду боту (обычно
                                 используется для действий вроде "Uninstall").
                  "url"        – кнопка-ссылка, открывает браузер.
                  "special"    – особая кнопка (пока только создание отчёта).
  text_key      : (строка) Ключ текста кнопки в файле STRINGS.py (словарь STRINGS).
                  Пример: "btn_create", "btn_uninstall".

  Дополнительные поля в зависимости от типа:

  * Если type = "callback":
      callback_data_template : (строка) Шаблон callback-данных. В нём можно
                               использовать {chat_id} и {thread_id} – они будут
                               автоматически заменены на реальные идентификаторы.
                               Пример: "w1_un_{chat_id}_{thread_id}"

  * Если type = "url":
      url_template           : (строка) Шаблон URL. Подстановки {chat_id}, {thread_id}
                               работают аналогично.
                               Пример: "https://example.com/group/{chat_id}"

  * Если type = "special":
      action                 : (строка) Какое специальное действие выполнить.
                               Сейчас поддерживается только "create_report" –
                               генерирует кнопку «Создать отчёт» с одноразовой
                               ссылкой для перехода в конструктор.
                               (Поле url_template для этого типа не требуется)

================================================================================
                        ПРИМЕРЫ И РЕКОМЕНДАЦИИ
================================================================================

1. Чтобы добавить новый текстовый блок, просто добавьте элемент в WIDGETS:
   {"type": "rules", "text_key": "dash_rules", "priority": 3, "is_permanent": True}
   (предварительно создайте строку "dash_rules" в файле STRINGS.py).

2. Чтобы убрать кнопку, удалите её ряд из BUTTONS или закомментируйте (поставив
   символ # в начале строки, но проще удалить весь ряд).

3. Чтобы поменять порядок кнопок, просто переставьте ряды местами.

================================================================================
                         (Планируется в будущем)
Тип виджета "image" – для вставки изображения-баннера. Поля:
  type = "image"
  file_id = "ID_файла_в_Telegram"   (получить можно через бота)
  caption_key = "ключ_подписи" (или None)
  priority = число
  is_permanent = True/False
Сейчас НЕ РАБОТАЕТ, зарезервировано для последующего расширения.
================================================================================




================================================================================
           СПРАВОЧНИК ДОСТУПНЫХ CALLBACK‑ДЕЙСТВИЙ ДЛЯ КНОПОК
================================================================================

Ниже перечислены все поддерживаемые на данный момент действия кнопок типа "callback".
Для каждого указан готовый шаблон callback_data_template, который можно скопировать
в конфигурацию кнопки.

  1. Деинсталляция бота из группы
     Шаблон: "m_dash_uninstall_{chat_id}_{thread_id}"
     Описание: Удаляет панель управления из текущего чата/топика.
              Требует подтверждения через диалоговое окно.

  2. Вызов справки
     Шаблон: "m_dash_help_{chat_id}_{thread_id}"
     Описание: Отправляет в личный чат администратора справочное сообщение
              со списком доступных команд (из STRINGS.py, ключ "help_text").

  3. Запланированные к внедрению (можно уже использовать, но обработчики
     находятся в разработке):
     a) Перезагрузка строк (Reload)
        Шаблон: "m_dash_reload_{chat_id}_{thread_id}"
        Описание: Перезагружает файлы STRINGS.py и main_dashboard_layout.py,
                 после чего обновляет дашборд.

     b) Мини‑игры
        Шаблон: "m_dash_minigames_{chat_id}_{thread_id}"
        Описание: Заглушка, выводит временное сообщение о недоступности.

     c) Открыть настройки
        Шаблон: "m_dash_settings_{chat_id}_{thread_id}"
        Описание: Открывает админ-панель в личном чате (аналогично /settings).

Чтобы добавить кнопку, просто вставьте её описание в нужный ряд списка BUTTONS,
используя подходящий шаблон из таблицы выше. Если действие не указано — кнопка
не будет работать; в этом случае обратитесь к разработчику за добавлением нового
обработчика.

"""

# ===================== НАСТРАИВАЙТЕ НИЖЕ =====================

WIDGETS = [
    # Приветственный блок (основной текст дашборда)
    {
        "type": "greeting",
        "text_key": "dash_main",        # Ключ в STRINGS
        "priority": 1,                  # Самый верх
        "is_permanent": True            # Не удаляется при обновлениях
    },

    # Техническая строка (версия и время последнего пинга)
    # Для этого виджета text_key = None – время генерируется автоматически
    {
        "type": "tech_bar",
        "text_key": None,
        "priority": 999,
        "is_permanent": True
    },
    
    # Заглушка для временных предупреждений (не удалять!)
    {
        "type": "alert_placeholder",
        "text_key": None,      # контент задаётся динамически
        "placeholder_text": "alert_placeholder_default",  # ключ в STRINGS или сырой текст
        "priority": 998,        # самый низ, чтобы быть внизу
        "is_permanent": True
    },

    # Вы можете добавить свои виджеты сюда.
    # Пример:
    # {
    #     "type": "info",
    #     "text_key": "dash_footer",
    #     "priority": 3,
    #     "is_permanent": True
    # },
]

# ===================== НАСТРАИВАЙТЕ НИЖЕ =====================

BUTTONS = [
    # Первый ряд: одна широкая кнопка «Создать отчёт»
    [
        {
            "type": "special",
            "action": "create_report",
            "text_key": "btn_create"
        }
    ],

    # Второй ряд: две кнопки рядом – «Uninstall» и «Справка»
    [
        {
            "type": "callback",
            "callback_data_template": "m_dash_reload_{chat_id}_{thread_id}",
            "text_key": "btn_reload_dash"
        },
        {
            "type": "callback",
            "callback_data_template": "m_dash_minigames_{chat_id}_{thread_id}",
            "text_key": "btn_mini_games_dash"
        }
    ],
    
    [
        {
            "type": "callback",
            "callback_data_template": "m_dash_settings_{chat_id}_{thread_id}",
            "text_key": "btn_settings"
        }
    ],

    # Здесь можно добавить дополнительные ряды.
    # Пример добавления кнопок «Reload» и «Mini-Games»:
    # [
    #     {
    #         "type": "callback",
    #         "callback_data_template": "m_dash_reload_{chat_id}_{thread_id}",
    #         "text_key": "btn_reload_dash"
    #     },
    #     {
    #         "type": "callback",
    #         "callback_data_template": "m_dash_minigames_{chat_id}_{thread_id}",
    #         "text_key": "btn_mini_games_dash"
    #     }
    # ],
]


### end of file ###